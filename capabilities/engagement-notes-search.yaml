naftiko: 1.0.0-alpha2
info:
  label: HubSpot CRM Engagement Notes API — Search
  description: 'HubSpot CRM Engagement Notes API — Search. 1 operations. Lead operation: Hubspot Search Notes. Self-contained
    Naftiko capability covering one Hubspot business surface.'
  tags:
  - Hubspot
  - Search
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    HUBSPOT_API_KEY: HUBSPOT_API_KEY
capability:
  consumes:
  - type: http
    namespace: engagement-notes-search
    baseUri: https://api.hubapi.com
    description: HubSpot CRM Engagement Notes API — Search business capability. Self-contained, no shared references.
    resources:
    - name: crm-v3-objects-notes-search
      path: /crm/v3/objects/notes/search
      operations:
      - name: searchnotes
        method: POST
        description: Hubspot Search Notes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.HUBSPOT_API_KEY}}'
  exposes:
  - type: rest
    namespace: engagement-notes-search-rest
    port: 8080
    description: REST adapter for HubSpot CRM Engagement Notes API — Search. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/crm/v3/objects/notes/search
      name: crm-v3-objects-notes-search
      description: REST surface for crm-v3-objects-notes-search.
      operations:
      - method: POST
        name: searchnotes
        description: Hubspot Search Notes
        call: engagement-notes-search.searchnotes
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: engagement-notes-search-mcp
    port: 9090
    transport: http
    description: MCP adapter for HubSpot CRM Engagement Notes API — Search. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: hubspot-search-notes
      description: Hubspot Search Notes
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: engagement-notes-search.searchnotes
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
