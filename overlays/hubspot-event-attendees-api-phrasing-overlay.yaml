# Generated by API Evangelist (build-phrasing.py). Our phrasing, not observed demand.
overlay: 1.0.0
info:
  title: API Evangelist conversational phrasing for Marketing Marketing Events Event Attendees API
  version: 1.0.0
extends: openapi/hubspot-event-attendees-api-openapi.yml
actions:
- target: $.info
  update:
    x-apievangelist-phrasing:
      method: generated
      generated: '2026-09-24'
      generator: build-phrasing.py
      label: Generated by API Evangelist
      operations: 4
- target: $.paths['/marketing/marketing-events/2026-09/attendance/{externalEventId}/{subscriberState}/create'].post
  update:
    x-apievangelist-phrasing:
      intent: Record attendance by contact ID for an external event
      effect: write
      questions:
      - How do I mark contacts as attending an event I track by my own external event ID, using their contact IDs?
      - Can I log join and leave times when recording contact IDs as attendees of an externally identified event?
      instructions:
      - text: Record contact IDs {inputs} as {subscriberState} for external event {externalEventId}.
        slots:
          inputs: requestBody.inputs
          subscriberState: path.subscriberState
          externalEventId: path.externalEventId
      - text: Log contact IDs {inputs} as {subscriberState} on external event {externalEventId} for external account {externalAccountId}.
        slots:
          inputs: requestBody.inputs
          subscriberState: path.subscriberState
          externalEventId: path.externalEventId
          externalAccountId: query.externalAccountId
      method: generated
      generated: '2026-09-24'
- target: $.paths['/marketing/marketing-events/2026-09/attendance/{externalEventId}/{subscriberState}/email-create'].post
  update:
    x-apievangelist-phrasing:
      intent: Record attendance by email for an external event
      effect: write
      questions:
      - Can I register attendees by email address for an event I identify with my own external event ID?
      - Will a new contact be created if an attendee's email isn't in the CRM yet when I use an external event ID?
      instructions:
      - text: Record attendee emails {inputs} as {subscriberState} for external event {externalEventId}.
        slots:
          inputs: requestBody.inputs
          subscriberState: path.subscriberState
          externalEventId: path.externalEventId
      - text: Mark emails {inputs} as {subscriberState} on external event {externalEventId} under external account {externalAccountId}.
        slots:
          inputs: requestBody.inputs
          subscriberState: path.subscriberState
          externalEventId: path.externalEventId
          externalAccountId: query.externalAccountId
      method: generated
      generated: '2026-09-24'
- target: $.paths['/marketing/marketing-events/2026-09/{objectId}/attendance/{subscriberState}/create'].post
  update:
    x-apievangelist-phrasing:
      intent: Record attendance by contact ID for a HubSpot event
      effect: write
      questions:
      - Can I mark contacts as registered, attended or cancelled using the marketing event's HubSpot object ID and their contact IDs?
      - Does recording contact IDs against a marketing event's internal ID add a timeline event to each contact?
      instructions:
      - text: Record contact IDs {inputs} as {subscriberState} on marketing event {objectId}.
        slots:
          inputs: requestBody.inputs
          subscriberState: path.subscriberState
          objectId: path.objectId
      - text: Mark contact IDs {inputs} as attended at HubSpot marketing event {objectId} with state {subscriberState}.
        slots:
          inputs: requestBody.inputs
          objectId: path.objectId
          subscriberState: path.subscriberState
      method: generated
      generated: '2026-09-24'
- target: $.paths['/marketing/marketing-events/2026-09/{objectId}/attendance/{subscriberState}/email-create'].post
  update:
    x-apievangelist-phrasing:
      intent: Record attendance by email for a HubSpot event
      effect: write
      questions:
      - Can I record attendees by email against a marketing event using its HubSpot object ID?
      - If I record emails on an event by its internal ID, are the contact properties I send applied to existing contacts?
      instructions:
      - text: Record attendee emails {inputs} as {subscriberState} on marketing event {objectId}.
        slots:
          inputs: requestBody.inputs
          subscriberState: path.subscriberState
          objectId: path.objectId
      - text: Register emails {inputs} for HubSpot marketing event {objectId} with state {subscriberState}, creating contacts that don't exist.
        slots:
          inputs: requestBody.inputs
          objectId: path.objectId
          subscriberState: path.subscriberState
      method: generated
      generated: '2026-09-24'
